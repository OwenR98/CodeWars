def sum_two_smallest_numbers(numbers):
    s = numbers.sort()
    count = 0
    x = numbers[count]
    
    while x < 0:
        count += 1
        x = numbers[count]
    
    f = x + numbers[count + 1]
    
    return f
