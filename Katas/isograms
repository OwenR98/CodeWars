from collections import Counter

def is_isogram(string):
    if string.isalpha() or string =='':
        count = Counter(string.upper())
        if len(count) == len(string):
            return True
        else:
            return False
    else:
        return False
