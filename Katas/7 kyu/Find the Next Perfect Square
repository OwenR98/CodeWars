import math

def find_next_square(sq):
    if math.sqrt(sq).is_integer():
        i = sq + 1
        while math.sqrt(i).is_integer() == False:
            i += 1
        return i
    else:
        return -1
    
