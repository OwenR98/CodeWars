def find_uniq(arr):
    ind = 0
    if arr[0] == arr[1]:
        while arr[ind] == arr[0]:
            ind += 1
        n = arr[ind]
        return n
    else:
        if arr[0] == arr[2]:
            n = arr[1]
            return n
        else:
            n = arr[0]
            return n
